首先，组件化中有三个参数：time表示每次发送后提示用户等待时长；descript表示等待时显示的信息，例如“请等待”字样；hasbtn表示是否需要倒计时按钮
在网页中输入手机号码，用正则对其合理性做了检验，不合理则提示手机号不正确，重新输入；手机号正确进行数据库检验，这里使用了json模拟，数据请求使用axios，watch监听变化
使用demo:
数据库中现存有13104013181手机号，可验证成功
运行代码前需要安装包 
npm i
运行命令：npm run dev
代码的测试命令：npm run unit
